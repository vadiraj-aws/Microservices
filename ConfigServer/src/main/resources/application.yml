server:
  port: 9296
  
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          default-label: public-view
          uri: https://github.com/vadiraj-aws/Microservices
          clone-on-start: true
          
          
#erueka:
#  instance:
#    prefer-ip-address: true
#  client:
#    register-with-eureka: true 
#    fetch-registry: true
#    service-url:
#      default-zone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
            